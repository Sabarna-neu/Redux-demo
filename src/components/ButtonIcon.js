import styled from 'styled-components'

const ButtonIcon = styled.button`
        border: none;
        cursor:pointer;
        padding:0.4rem;
        margin:0 0.2rem;
        background: none;
    `;


export default ButtonIcon;